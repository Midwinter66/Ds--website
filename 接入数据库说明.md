# 冬 · 接入 Supabase 云同步

改造已完成，代码已就位。**你只需要做 3 步配置**，不用改任何业务逻辑。

---

## 现状：改了什么

| 文件 | 说明 |
|---|---|
| `js/config.js` | **新增**，填 Supabase URL 和 anon key |
| `js/cloud-store.js` | **新增**，云同步核心（镜像、合并、冲突处理、离线降级） |
| `js/desk.js` | **新增**，工作台入口 + 顶栏同步指示 + 设置页面板 |
| `css/desk.css` | **新增**，工作台全部组件样式（原先完全没有，模块根本没法看） |
| `supabase/schema.sql` | **新增**，建表 + RLS 策略 + 同步函数 |
| `index.html` | 加了「进入工作台」入口、侧栏/顶栏/内容容器、17 个 script 标签 |
| `js/app.js` `js/router.js` | 侧栏类名 `nav-item` → `desk-nav-item`（避开与博客分类筛选的同名冲突） |
| `js/winter-app.js` | Enter 键在进入工作台后不再误触发博客入口 |
| `index.html` | 删掉了暴露在首页的模板文章卡（`posts/_template/`，内容是「文章标题 / 标签1」占位符，且页面根本不存在） |

---

## 第一步：建 Supabase 项目

1. 打开 https://supabase.com 注册（GitHub 账号可直接登）
2. New project，区域选 **Singapore（东南亚）**，离国内最近
3. 密码随便设一个，自己记下来就行（用不到）

## 第二步：执行建表 SQL

左侧 **SQL Editor** → New query → 把 `supabase/schema.sql` 整段粘进去 → Run。

会创建：

- 表 `user_store`（`user_id` + `key` 联合主键，`value` 存 JSON）
- 4 条 RLS 策略：只能读写自己的数据
- 函数 `sync_store()`：按时间戳做「最后写入者胜」合并
- 函数 `pull_store()`：全量拉取

## 第三步：关掉邮箱验证（可选，个人站建议关）

**Authentication** → **Providers** → **Email** → 关掉 **Confirm email**。

不关的话注册后要去邮箱点确认链接才能登录。

## 第四步：填配置

**Project Settings** → **API**，复制两个值：

- `Project URL` → 填进 `js/config.js` 的 `supabaseUrl`
- `anon public` key → 填进 `supabaseAnonKey`

```js
window.APP_CONFIG = {
  supabaseUrl: 'https://xxxxx.supabase.co',
  supabaseAnonKey: 'eyJhbGciOi...'
};
```

> anon key 本来就是公开的，安全靠 RLS 策略兜底。**绝对不要填 `service_role` 那个 key**。

## 第五步：部署 + 首次登录

```bash
git add .
git commit -m "feat: 工作台模块 + Supabase 云同步"
git push origin gh-pages
```

等 GitHub Pages 构建完（约 1 分钟），打开网站：

1. 入口页点 **进入工作台 · Desk**
2. 左下角 **数据和设置** → 云同步面板 → **注册**（填邮箱 + 6 位以上密码）
3. 注册完自动登录并首次同步

---

## 同步机制

```
本地改动 → Storage.set() → 写 localStorage + 记录时间戳
                        ↓ 1.2 秒防抖
                   推到 Supabase（RPC sync_store，按时间戳判新旧）

启动时 / 每 60 秒 / 页面重新可见 / 手动点击 → 拉全量
                    ↓ 逐键比较 updated_at
          云端新 → 覆盖本地并刷新界面
          本地新 → 推上去
```

**冲突处理**：以「最后写入者胜」为准，粒度是**整个键**（比如 `app_tasks` 整体）。
个人单用户场景足够用；如果同一账号多设备同时狂改同一模块，后写的会覆盖先写的。

**会同步的键**：`app_tasks` `app_research` `app_psychology` `app_fitness` `app_games` `app_cooking` `app_memos` `app_settings`
**不同步**：`app_psychology_tab`（纯界面状态）

## 降级行为

| 情况 | 表现 |
|---|---|
| `config.js` 留空 | 纯本地模式，一切照旧，顶栏显示「本地模式」 |
| 未登录 | 「未登录」状态，数据仍存本地 |
| 断网 | 「离线待同步」，改动留在本地，恢复网络后自动重传 |
| SDK 加载失败 | 自动降级本地模式，不白屏 |

## 顶栏状态灯

| 灯 | 含义 |
|---|---|
| 灰 · 本地模式 | 没填 config |
| 灰 · 未登录 | 填了但没登录 |
| 金色闪烁 · 同步中 | 正在上传/拉取 |
| 绿色 · 已同步 | 一切正常，后面跟时间 |
| 红色 · 离线待同步 | 网络或鉴权出错，改动已留本地 |

点状态灯可以手动触发一次同步。

---

## 本地开发验证

```bash
# 仓库根目录起服务（路径是 /Ds--website/，所以要在上一级起）
cd ..
python -m http.server 8765
# 打开 http://127.0.0.1:8765/Ds--website/
```

跑冒烟测试（需要 jsdom）：

```bash
npm install jsdom
node tools/smoke-test.js
```

会加载首页、遍历 8 个模块、点开两个小游戏、模拟入口切换，输出 41 项检查结果。

> 实现细节：jsdom 的 `resources:'usable'` 会并行拉取脚本且**不保证按文档顺序执行**，而本页面强依赖顺序（storage → router → modules → app → desk）。所以测试脚本先把 `<script>` 摘出来，再串行注入。真实浏览器无此问题。

---

## 已知限制

1. **冲突粒度是整键**，不是字段级。多设备并发编辑会整体覆盖。
2. 文章仍是硬编码。想让文章也进数据库，是另一个话题（建议改用 Markdown + 构建，或直接接 Supabase 的 `posts` 表）。
